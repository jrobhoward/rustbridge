[template]
cargo_generate_version = ">=0.18.0"
ignore = [
    ".git",
    "target",
]

[placeholders.completed]
type = "bool"
prompt = "Include completed regex implementation? (false = basic echo template)"
default = false

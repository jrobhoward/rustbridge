[plugin]
name = "{{project-name}}"
version = "0.1.0"
description = "A rustbridge plugin"

[messages."echo"]
description = "Echo the input message back with its length"

[platforms]
linux-x86_64 = "lib{{package-name}}.so"
darwin-aarch64 = "lib{{package-name}}.dylib"
darwin-x86_64 = "lib{{package-name}}.dylib"
windows-x86_64 = "{{package-name}}.dll"

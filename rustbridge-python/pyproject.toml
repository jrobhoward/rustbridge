[project]
name = "rustbridge"
version = "1.0.0"
description = "Python bindings for rustbridge - Rust shared libraries callable from Python"
readme = "README.md"
license = {text = "MIT OR Apache-2.0"}
requires-python = ">=3.10"
authors = [
    { name = "RustBridge Authors" }
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Libraries",
]
keywords = ["rust", "ffi", "plugin", "native"]
dependencies = [
    "PyNaCl>=1.5.0",
]

[project.urls]
Homepage = "https://github.com/jrobhoward/rustbridge"
Repository = "https://github.com/jrobhoward/rustbridge"
Documentation = "https://github.com/jrobhoward/rustbridge/tree/main/docs"
Issues = "https://github.com/jrobhoward/rustbridge/issues"
Changelog = "https://github.com/jrobhoward/rustbridge/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-benchmark>=5.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["rustbridge*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
